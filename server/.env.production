## Backend production environment
NODE_ENV=production

# Primary production connection (Azure Postgres)
POSTGRES_URI=postgresql://rakshit:AzureAdmin%401234@erp-db.postgres.database.azure.com:5432/postgres?sslmode=require

# Fallback discrete values (if POSTGRES_URI missing)
DB_HOST=erp-db.postgres.database.azure.com
DB_PORT=5432
DB_USER=rakshit
DB_PASSWORD=AzureAdmin@1234
DB_NAME=postgres

# Security / Auth
JWT_SECRET=prod_6ae3d4e4c6b5420cb4e4b6d9b1f7b71e_9f8f7d6c
JWT_EXPIRES_IN=7d

# Server (Azure injects PORT dynamically; keep fallback)
PORT=8080

# CORS (point to your Netlify frontend domain)
CORS_ORIGIN=https://asuerp.netlify.app
