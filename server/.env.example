# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=yarn_erp
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
# Note: Frontend typically runs on port 5173 or 5174 with Vite
# For production, use your actual domain:
# CORS_ORIGIN=https://yourdomain.com
# For multiple origins, you'll need to modify server code to handle array
